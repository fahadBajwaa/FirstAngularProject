import { Routes } from '@angular/router'; // Import the type for defining route arrays

export const routes: Routes = []; // Each route links a URL path to a Component

//example code:

// export const routes: Routes = [
//   // Example:
//   // { path: 'home', component: HomeComponent },
//   // { path: '', redirectTo: 'home', pathMatch: 'full' }
// ];
